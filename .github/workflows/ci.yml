name: CI

on:
  push:
    branches:
      - master
      - main
  pull_request:
    branches:
      - master
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: corepack pnpm install --frozen-lockfile

      - name: Type check workspace
        run: corepack pnpm -r typecheck

      - name: Run tests
        run: corepack pnpm -r test

      - name: Build workspace
        run: corepack pnpm --filter @salt-sync/shared --filter @salt-sync/server --filter @salt-sync/plugin build

      - name: Check plugin bundle safety
        run: corepack pnpm --filter @salt-sync/plugin check:bundle-safety

      - name: Upload plugin bundle artifact
        uses: actions/upload-artifact@v4
        with:
          name: salt-sync-plugin-dist
          path: |
            packages/plugin/dist/main.js
            packages/plugin/manifest.json
            packages/plugin/styles.css
